export interface CodesSettings {
    [code: string]: {
        inline: string;
        className: string;
    };
}
export declare const COLOR_CODES: CodesSettings;
export declare const STYLE_CODES: CodesSettings;
